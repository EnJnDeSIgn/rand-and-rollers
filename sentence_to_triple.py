def sentence_to_triple(sentence):
    # 1. tokenize and POS-tag
    # 2. identify subject, verb, object (semantic role labeling)
    # 3. extract qualifiers (adjectives, adverbs, prepositional phrases)
    return (subject, predicate, object, qualifiers)
